import { Typography } from "@/components/typography";

const Dashboard = () => {
  return (
    <div className="h-screen w-full flex items-center justify-center">
      <Typography variant="Bold_H2">Welcome to dashboard</Typography>
    </div>
  );
};

export default Dashboard;
